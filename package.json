{
  "name": "csv-blocks",
  "version": "1.0.0",
  "private": true,
  "description": "A superset of CSV designed as a simple, intuitive, human-editable intermediate format for data imports.",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/lib.mjs"
    }
  },
  "bin": {
    "csv-blocks": "src/cli.mjs",
    "csvb": "src/cli.mjs"
  },
  "scripts": {
    "ci": "npm run test",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "csv-parse": "^6.1.0",
    "duplexify": "^4.1.3"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
